#include <bits/stdc++.h>
#define FASTIO ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);

using namespace std;
using ll = long long;

int height[20001];

int main() {
	FASTIO;

	int C;
	cin >> C;

	while (C--) {
		int N;
		cin >> N;

		for (int i = 1; i <= N; i++) {
			cin >> height[i];
		}


	}

	return 0;
}